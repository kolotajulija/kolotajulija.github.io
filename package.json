{
  "name": "julijakolota-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "bump": "node scripts/bump-version.mjs",
    "build": "tsc --noEmit && vite build",
    "release": "node scripts/bump-version.mjs && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
