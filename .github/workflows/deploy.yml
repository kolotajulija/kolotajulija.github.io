name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      # Репозиторий называется kolotajulija.github.io, поэтому сайт живёт
      # в корне: https://kolotajulija.github.io/ — base остаётся /.
      # Если код переедет в обычный репозиторий, здесь нужно будет
      # поставить BASE_PATH: /<имя-репозитория>/
      - run: npm run build
        env:
          BASE_PATH: /

      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
